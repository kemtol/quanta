name = "fut-features"
main = "src/index.js"
compatibility_date = "2024-11-01"

# KV namespace for cron state (shared with cron-checker)

# R2 bucket binding
[[r2_buckets]]
binding = "TAPE_DATA_FUTURES"
bucket_name = "tape-data-futures"

# NOTE: Cron trigger REMOVED - now managed by cron-checker
# Old config (DO NOT ADD):
# [triggers]
# crons = ["15 * * * *"]
